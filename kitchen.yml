---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: windows-2019
    driver_config:
      box: tas50/windows_2019

suites:
  - name: default
    run_list:
      - recipe[notepadpp]
      - recipe[minitest-handler]

verifier:
  name: inspec
